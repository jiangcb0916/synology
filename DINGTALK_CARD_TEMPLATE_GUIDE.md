# 钉钉卡片模板完整指南

## 📋 目录

1. [可用的变量](#可用的变量)
2. [添加组件](#添加组件)
3. [变量格式要求](#变量格式要求)
4. [常见问题与解决方法](#常见问题与解决方法)
5. [验证与测试](#验证与测试)

---

## 📋 可用的变量

根据代码，以下变量可以在模板中使用：

| 变量名 | 说明 | 示例值 | 可用时机 |
|--------|------|--------|----------|
| `name` | 用户姓名 | "蒋成博" | 创建和更新时 |
| `createTime` | 创建时间 | "2026-01-26 13:10:35" | 创建和更新时 |
| `status` | 审批状态 | "待审批" / "已同意" / "已拒绝" | 创建和更新时 |
| `title` | 卡片标题 | "蒋成博 的账户创建审批" | 创建和更新时 |
| `lastMessage` | 最后一条消息 | "账户创建审批：蒋成博" | 创建和更新时 |
| `applicant` | 申请人 | "管理员姓名" | 仅创建时 |
| `operator` | 操作者 | "操作者姓名" | 仅更新时（可选） |

---

## 🎨 添加组件

### 步骤1：打开组件库

1. 在模板编辑器的左侧或顶部找到**组件库**或**组件面板**
2. 常见的组件类型包括：
   - **文本组件** - 显示文本信息
   - **图片组件** - 显示图片
   - **按钮组件** - 交互按钮
   - **容器组件** - 布局容器
   - **分割线组件** - 分隔内容

### 步骤2：添加文本组件显示审批信息

#### 建议添加的审批信息：

1. **申请人信息**
   - 组件：文本组件
   - 显示内容：`申请人：{{applicant}}`
   - 位置：放在标题下方

2. **用户姓名**
   - 组件：文本组件
   - 显示内容：`用户姓名：{{name}}`
   - 位置：放在申请人信息下方

3. **创建时间**
   - 组件：文本组件
   - 显示内容：`创建时间：{{createTime}}`
   - 位置：放在用户姓名下方

4. **审批状态**（重要）
   - 组件：文本组件
   - 显示内容：`审批状态：{{status}}`
   - 位置：放在创建时间下方
   - 样式：可以根据状态值设置不同颜色

5. **操作者信息**（审批后显示）
   - 组件：文本组件
   - 显示内容：`操作者：{{operator}}`
   - 显示条件：`status === "已同意" || status === "已拒绝"`
   - 位置：放在审批状态下方

### 具体操作步骤

#### 方法1：使用文本组件

1. **从组件库拖拽文本组件**
   - 在左侧组件库中找到"文本"组件
   - 拖拽到卡片预览区域

2. **设置文本内容**
   - 选中刚添加的文本组件
   - 在右侧属性面板中找到"内容"或"文本"字段
   - 输入：`申请人：{{applicant}}`
   - **重要**：使用 `{{变量名}}` 格式来绑定变量（双大括号）

3. **设置样式（可选）**
   - 在"样式"标签页中设置字体大小、颜色等
   - 例如：标题可以用较大字体，内容用正常字体

4. **设置显示条件（可选）**
   - 在"显示控制"中设置条件
   - 例如：操作者信息只在审批后显示

#### 方法2：使用变量选择器（推荐）

1. **选中文本组件**
2. **在内容输入框中输入 `$`（美元符号）**
3. **从弹出的变量选择器中选择变量**
4. **会自动插入正确格式的 `{{variable}}`**

这样可以避免手动输入错误，确保变量格式正确。

### 推荐的卡片布局结构

```
┌─────────────────────────────────┐
│  🟠 审批                          │
├─────────────────────────────────┤
│  申请人：{{applicant}}            │
│  用户姓名：{{name}}               │
│  创建时间：{{createTime}}         │
│  审批状态：{{status}}             │
│  ─────────────────────────────   │
│  [拒绝]  [同意]                   │
│  ─────────────────────────────   │
│  已同意 / 已拒绝                  │
└─────────────────────────────────┘
```

---

## ⚠️ 变量格式要求

### 正确的变量格式

**必须使用双大括号格式**：`{{变量名}}`

| 格式 | 效果 | 说明 |
|------|------|------|
| `{{applicant}}` | ✅ 显示实际值 | 双大括号，正确 |
| `{applicant}` | ❌ 显示变量名 | 单大括号，错误 |
| `applicant` | ❌ 显示文本 | 无大括号，错误 |
| `{{applicant` | ❌ 无法解析 | 缺少闭合括号，错误 |

### 正确的格式示例

```
申请人: {{applicant}}
用户姓名: {{name}}
创建时间: {{createTime}}
审批状态: {{status}}
操作者: {{operator}}
```

### 错误的格式示例

```
申请人: {applicant}          ❌ 单大括号
创建时间: {{createTime        ❌ 缺少闭合括号
审批状态: createTime          ❌ 无大括号
```

### 重要提示

1. **必须使用双大括号**
   - ✅ 正确：`{{variable}}`
   - ❌ 错误：`{variable}`

2. **大括号必须成对出现**
   - ✅ 正确：`{{` 和 `}}`
   - ❌ 错误：`{{` 和 `}` 或 `{` 和 `}}`

3. **变量名区分大小写**
   - `{{applicant}}` 正确
   - `{{Applicant}}` 错误（如果代码中使用的是小写）

---

## 🔧 常见问题与解决方法

### 问题1：卡片显示变量名而不是实际值

#### 情况A：在编辑模式下查看（正常）

**现象**：
- 在模板编辑器中看到 `{{applicant}}` 等变量名
- 这是**正常现象**

**原因**：
- 模板编辑器默认显示变量占位符
- 这是为了让你看到哪些地方使用了变量
- 实际发送卡片时，这些变量会被替换为真实值

**解决方法**：
- 切换到**预览模式**查看实际效果
- 或直接发送实际卡片来验证

#### 情况B：预览模式显示变量名

**现象**：
- 切换到预览模式后仍然显示 `{{applicant}}` 等变量名

**原因**：
- 预览模式需要手动输入测试数据
- 如果没有输入，就会显示变量名

**解决方法**：

1. **在预览模式中设置测试数据**
   - 查找"测试数据"或"预览数据"设置
   - 输入以下JSON格式的数据：
     ```json
     {
       "applicant": "管理员姓名",
       "name": "蒋成博",
       "createTime": "2026-01-26 13:10:35",
       "status": "待审批",
       "title": "蒋成博 的账户创建审批",
       "lastMessage": "账户创建审批：蒋成博"
     }
     ```
   - 点击"应用"或"确定"按钮

2. **如果找不到测试数据设置**
   - 某些编辑器版本可能不支持预览测试数据
   - 直接测试实际发送（最可靠的方法）

#### 情况C：实际卡片显示变量名

**现象**：
- 实际发送的卡片中显示 `{{applicant}}` 或 `{applicant}` 等变量名

**可能原因**：

1. **使用了错误的变量格式**
   - 使用了单大括号 `{variable}` 而不是双大括号 `{{variable}}`
   - 或变量格式不完整

2. **变量名不匹配**
   - 模板中使用的变量名与代码中发送的不一致
   - 变量名大小写不匹配

3. **变量未定义**
   - 模板的变量定义区域中没有定义该变量

**解决方法**：

1. **检查并修复变量格式**
   - 确保所有变量都使用 `{{variable}}` 格式（双大括号）
   - 检查是否有变量缺少闭合括号

2. **检查变量名**
   - 确认变量名与代码中使用的完全一致（区分大小写）
   - 参考[可用的变量](#可用的变量)表格

3. **检查变量定义**
   - 在模板的变量定义区域确认所有变量都已定义

### 问题2：变量格式不完整

**现象**：
- 变量缺少闭合的大括号，如 `{{createTime` 而不是 `{{createTime}}`

**解决方法**：

1. **选中组件**
   - 在卡片预览中点击有问题的组件

2. **修复变量格式**
   - 在右侧属性面板的"内容"字段中
   - 将 `{{createTime` 改为 `{{createTime}}`
   - 确保有闭合的 `}}`

3. **检查所有变量**
   - 逐一检查所有文本组件中的变量格式
   - 确保所有变量都使用完整格式

### 问题3：部分变量显示，部分不显示

**可能原因**：
- 某些变量未定义
- 变量名大小写不匹配
- 变量格式不一致

**解决方法**：
1. 检查模板的变量定义区域
2. 确认所有变量都已定义
3. 检查变量名大小写
4. 统一使用 `{{variable}}` 格式

### 问题4：预览模式没有测试数据设置

**原因**：某些编辑器版本可能不支持预览测试数据

**解决**：直接测试实际发送，这是最可靠的方法

---

## ✅ 验证与测试

### 方法1：在预览模式中测试

1. **切换到预览模式**
   - 点击编辑器顶部的"预览模式"按钮

2. **设置测试数据**
   - 找到"测试数据"或"预览数据"设置
   - 输入测试值（参考上面的JSON格式）

3. **查看预览效果**
   - 应该看到实际的值，而不是 `{{variable}}`

### 方法2：实际发送卡片测试（最可靠）

1. **保存模板**
   - 在编辑器中保存模板

2. **运行程序**
   ```bash
   python3 main.py
   ```

3. **发送一个审批卡片**
   - 在钉钉中发送创建用户的请求
   - 卡片会被发送到指定用户

4. **查看实际卡片**
   - 在钉钉中打开收到的卡片
   - 应该看到实际的值：
     ```
     申请人: 管理员姓名
     用户姓名: 蒋成博
     创建时间: 2026-01-26 13:10:35
     审批状态: 待审批
     ```

### 正常显示效果对比

#### 在编辑器中（显示变量名）：
```
申请人: {{applicant}}
用户姓名: {{name}}
创建时间: {{createTime}}
审批状态: {{status}}
```
**这是正常的！** 编辑器显示变量占位符。

#### 在实际卡片中（显示实际值）：
```
申请人: 管理员姓名
用户姓名: 蒋成博
创建时间: 2026-01-26 13:10:35
审批状态: 待审批
```
**这才是最终效果！**

### 验证清单

修复后，检查：

- [ ] 所有变量都使用 `{{variable}}` 格式（双大括号）
- [ ] 没有使用 `{variable}` 格式（单大括号）
- [ ] 所有变量格式完整（有闭合括号）
- [ ] 变量名与代码中使用的完全一致（区分大小写）
- [ ] 所有变量都在变量定义区域定义
- [ ] 保存模板
- [ ] 重新发送测试卡片
- [ ] 验证实际显示效果

### 快速验证方法

运行以下命令查看代码中实际发送的数据：

```bash
# 查看日志
tail -f logs/*.log | grep "准备更新卡片数据"
```

日志会显示实际发送的变量名和值，可以用来对比模板中的变量名。

---

## 📝 总结

### 关键要点

1. **变量格式**：必须使用 `{{variable}}` 格式（双大括号）
2. **变量名**：必须与代码中使用的完全一致（区分大小写）
3. **变量定义**：所有变量都必须在模板的变量定义区域定义
4. **测试方法**：最可靠的方法是直接测试实际发送

### 常见错误

- ❌ 使用单大括号 `{variable}`
- ❌ 变量格式不完整 `{{variable`
- ❌ 变量名大小写不匹配
- ❌ 变量未定义

### 正确做法

- ✅ 使用双大括号 `{{variable}}`
- ✅ 使用变量选择器（输入 `$`）自动插入
- ✅ 检查所有变量格式
- ✅ 实际测试验证效果

按照本指南操作，你的钉钉卡片模板应该能够正确显示所有变量值！

