# 微爱基金会 Synology 用户管理系统 - 技术文档

**项目名称**: 微爱基金会 Synology 用户管理系统  
**版本**: v6.0.0   
**状态**: ✅ 生产就绪

---

## 项目概述

### 背景
创建群晖用户需要手动登录群晖创建用户、加入组等操作，对于大部分同事来说操作困难。本系统通过钉钉机器人自动化完成用户创建流程，简化操作步骤。

### 系统简介
基于钉钉机器人的 Synology NAS 用户自动创建和管理系统，支持审批流程和权限控制。系统通过钉钉 Stream 模式长连接接收消息，自动在 Synology NAS 上创建用户账户，并将账户信息通过私聊发送给用户。

### 核心特性

- 🤖 **钉钉机器人集成** - 通过钉钉 Stream 模式长连接与用户交互
- 👥 **自动用户创建** - 根据姓名自动创建 Synology 用户账户（自动生成用户名和密码）
- 📋 **用户列表查询** - 支持查询 Synology 上的所有用户列表
- 🔐 **权限管理** - 管理员和审批人分离，只有管理员可以发起创建请求
- 📝 **审批流程** - 支持钉钉卡片审批流程（可选，需配置卡片模板ID）
- 🔒 **安全性** - 敏感信息（用户名、密码）仅通过私聊发送，不在群聊中暴露
- 📊 **智能查找** - 自动在钉钉组织内查找用户（支持多种查找策略）
- 🔄 **双模式支持** - 支持审批模式和直接创建模式

### 技术栈

- **语言**: Python 3.7+
- **框架**: dingtalk-stream SDK (Stream 模式长连接)
- **通信**: SSH (Paramiko) 连接 Synology NAS
- **依赖管理**: pip + requirements.txt
- **第三方库**:
  - `dingtalk-stream`: 钉钉 Stream SDK
  - `paramiko`: SSH 连接
  - `pypinyin`: 中文姓名转拼音
  - `alibabacloud-dingtalk`: 钉钉卡片 SDK
  - `requests`: HTTP 请求

---

## 系统架构

### 业务流程

#### 审批模式（配置了卡片模板ID）
```
用户发送消息 → 权限检查（管理员） → 查找钉钉用户 → 发送审批卡片给审批人 
→ 审批人审批 → 卡片回调处理 → 创建 Synology 账户 → 发送账户信息给用户
```

#### 直接创建模式（未配置卡片模板ID）
```
用户发送消息 → 权限检查（管理员） → 查找钉钉用户 → 直接创建 Synology 账户 
→ 发送账户信息给用户
```

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      钉钉开放平台                              │
│                (Stream 模式 - 长连接)                          │
└─────────────────┬───────────────────────────────┬─────────────┘
                  │                               │
                  │ 聊天消息                       │ 卡片回调
                  ↓                               ↓
┌─────────────────────────────────────────────────────────────┐
│                    DingTalk Stream Client                   │
│                    (dingtalk-stream SDK)                    │
│                    (src/app.py: DingTalkBotApp)            │
└─────────────────┬───────────────────────────────┬─────────────┘
                  │                               │
                  ↓                               ↓
         ┌─────────────────┐           ┌──────────────────────┐
         │ ChatbotHandler  │           │ CardCallbackHandler  │
         │  (消息处理器)    │           │   (卡片回调处理器)    │
         │  - 权限检查      │           │  - 审批权限检查        │
         │  - 意图解析      │           │  - 审批通过/拒绝      │
         │  - 用户创建      │           │  - 账户创建           │
         │  - 用户列表查询  │           │  - 状态更新           │
         └────────┬────────┘           └──────────┬───────────┘
                  │                               │
                  └───────────┬───────────────────┘
                              │
                              ↓
         ┌────────────────────────────────────────┐
         │          Service Layer (服务层)         │
         │  ┌──────────────────────────────────┐  │
         │  │  DingTalkAPIService              │  │
         │  │  - Token 管理和缓存              │  │
         │  │  - Access Token 自动刷新         │  │
         │  └───────────┬──────────────────────┘  │
         │              │                          │
         │  ┌───────────┴──────────────────────┐  │
         │  │  DingTalkUserService             │  │
         │  │  - 根据姓名查找用户ID             │  │
         │  │  - 根据ID获取用户姓名             │  │
         │  │  - 多策略查找（搜索API/部门遍历）  │  │
         │  └──────────────────────────────────┘  │
         │  ┌──────────────────────────────────┐  │
         │  │  DingTalkMessageService          │  │
         │  │  - 私聊消息发送                   │  │
         │  │  - 群聊消息发送                   │  │
         │  │  - Markdown 消息格式化            │  │
         │  └──────────────────────────────────┘  │
         │  ┌──────────────────────────────────┐  │
         │  │  DingTalkCardService             │  │
         │  │  - 审批卡片发送                   │  │
         │  │  - 卡片状态更新                   │  │
         │  │  - 待审批卡片管理                 │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          │
                          ↓
         ┌────────────────────────────────────────┐
         │       Core Layer (核心业务层)           │
         │  ┌──────────────────────────────────┐  │
         │  │  SynologyService                 │  │
         │  │  - SSH 连接管理                   │  │
         │  │  - 用户创建（synouser命令）       │  │
         │  │  - 用户存在性检查                 │  │
         │  │  - 用户组管理（vlf组）             │  │
         │  │  - 用户列表查询                   │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          │
                          ↓ SSH 连接 (Paramiko)
         ┌────────────────────────────────────────┐
         │          Synology NAS                  │
         │      (DSM 7.x 用户管理系统)             │
         │  - synouser 命令创建用户                │
         │  - synogroup 命令管理用户组             │
         └────────────────────────────────────────┘
```

### 分层架构

```
┌─────────────────────────────────┐
│    Presentation Layer           │  ← handlers/ (处理器层)
│    - ChatbotHandler             │     - 消息处理
│    - CardCallbackHandler        │     - 权限检查
├─────────────────────────────────┤     - 意图解析
│    Service Layer                │  ← services/ (服务层)
│    - DingTalkAPIService         │     - 钉钉API封装
│    - DingTalkUserService        │     - 用户查找
│    - DingTalkMessageService     │     - 消息发送
│    - DingTalkCardService        │     - 卡片管理
├─────────────────────────────────┤
│    Business Logic Layer         │  ← core/ (核心业务层)
│    - SynologyService            │     - Synology用户管理
├─────────────────────────────────┤
│    Infrastructure Layer         │  ← utils/ (工具层)
│    - NameUtils                  │     - 姓名转拼音
│    - PasswordGenerator          │     - 密码生成
│    - MessageFormatter           │     - 消息格式化
├─────────────────────────────────┤
│    Configuration Layer          │  ← config/ (配置层)
│    - Settings                   │     - 配置管理
└─────────────────────────────────┘
```

### 目录结构

```
synology/
├── main.py                    # 主入口文件
├── config.ini                 # 配置文件（不提交到版本控制）
├── config.ini.example         # 配置示例
├── requirements.txt           # 依赖包列表
├── README.md                  # 项目说明
├── .gitignore                 # Git忽略配置
├── logs/                      # 日志目录
├── tests/                     # 测试目录
└── src/                       # 源代码目录
    ├── app.py                 # 应用主类（DingTalkBotApp）
    ├── config/                # 配置管理模块
    │   └── settings.py        # 配置读取和验证
    ├── core/                  # 核心业务模块
    │   └── synology_service.py # Synology用户管理
    ├── services/              # 服务层模块
    │   ├── dingtalk_api_service.py      # 钉钉API服务（Token管理）
    │   ├── dingtalk_user_service.py    # 钉钉用户服务（用户查找）
    │   ├── dingtalk_message_service.py  # 钉钉消息服务（消息发送）
    │   └── dingtalk_card_service.py     # 钉钉卡片服务（卡片管理）
    ├── handlers/              # 处理器模块
    │   ├── chatbot_handler.py           # 聊天机器人消息处理器
    │   └── card_callback_handler.py     # 卡片回调处理器
    ├── models/                # 数据模型（预留）
    └── utils/                 # 工具模块
        ├── name_utils.py              # 姓名转拼音工具
        ├── password_generator.py      # 密码生成工具
        └── message_formatter.py       # 消息格式化工具
```

---

## 功能特性

### 用户创建流程

#### 审批模式（推荐）
1. 管理员发送创建请求（格式：`姓名` 或 `创建 姓名`）
2. 系统检查管理员权限
3. 在钉钉组织内查找目标用户
4. 发送审批卡片给审批人
5. 审批人点击"通过"或"拒绝"
6. 如果通过，系统创建 Synology 账户
7. 将账户信息（用户名、密码、挂载脚本链接）通过私聊发送给用户
8. 如果是群消息，同时发送群回复（不含敏感信息）

#### 直接创建模式
1. 管理员发送创建请求
2. 系统检查管理员权限
3. 在钉钉组织内查找目标用户
4. 直接创建 Synology 账户
5. 将账户信息通过私聊发送给用户

### 用户列表查询

管理员可以发送 `用户列表` 或 `查看用户` 来查询 Synology 上的所有用户。

### 核心模块详解

#### 1. 配置层 (config/)

**Settings** (`src/config/settings.py`)
- 统一配置管理
- 配置文件读取和验证
- 类型安全的属性访问
- 默认值处理
- 支持配置项：
  - Synology: url, host, username, password, ssh_port
  - 钉钉: client_id, client_secret, card_template_id, admin_name, approver_name

#### 2. 核心业务层 (core/)

**SynologyService** (`src/core/synology_service.py`)
- SSH 连接管理（使用 Paramiko）
- 用户创建：使用 `synouser` 命令创建用户
- 用户组管理：自动将用户加入 `vlf` 组
- 用户存在性检查：使用 `id` 命令检查
- 用户列表查询：解析 `/etc/passwd` 文件
- 自动生成用户名：中文姓名转拼音（使用 pypinyin）
- 自动生成密码：8位随机字母数字组合

#### 3. 服务层 (services/)

**DingTalkAPIService** (`src/services/dingtalk_api_service.py`)
- Access Token 管理和缓存
- 自动刷新 Token（提前5分钟刷新）
- Token 有效期：7200秒

**DingTalkUserService** (`src/services/dingtalk_user_service.py`)
- 根据姓名查找钉钉用户 ID
- 根据用户 ID 获取用户姓名
- 多策略查找：
  1. 使用搜索 API (`smartwork/hrm/employee/queryonjob`)
  2. 遍历部门查找 (`v2/department/listsub` + `v2/user/list`)

**DingTalkMessageService** (`src/services/dingtalk_message_service.py`)
- 私聊消息发送（Markdown格式）
- 群聊消息发送（Markdown格式）
- 批量发送接口支持
- 自动降级处理（Markdown失败时尝试文本格式）

**DingTalkCardService** (`src/services/dingtalk_card_service.py`)
- 审批卡片发送（使用 alibabacloud-dingtalk SDK）
- 卡片状态更新（已同意/已拒绝）
- 待审批卡片管理（内存存储）
- 异步卡片操作支持

#### 4. 处理器层 (handlers/)

**ChatbotHandler** (`src/handlers/chatbot_handler.py`)
- 消息接收和处理
- 权限检查（管理员权限）
- 意图解析：
  - `创建用户`：识别创建用户意图
  - `用户列表`：识别查询用户列表意图
- 用户创建流程控制
- 用户列表查询处理
- 支持私聊和群聊消息

**CardCallbackHandler** (`src/handlers/card_callback_handler.py`)
- 卡片回调处理
- 审批权限检查（审批人权限）
- 审批通过处理：创建账户并发送通知
- 审批拒绝处理：发送拒绝通知
- 卡片状态更新

#### 5. 工具层 (utils/)

**NameUtils** (`src/utils/name_utils.py`)
- 中文姓名转拼音（使用 pypinyin）
- 生成小写拼音用户名

**PasswordGenerator** (`src/utils/password_generator.py`)
- 随机密码生成
- 默认8位，包含字母和数字

**MessageFormatter** (`src/utils/message_formatter.py`)
- 各种消息格式化：
  - 成功消息（包含账户信息和挂载脚本链接）
  - 错误消息
  - 用户已存在消息
  - 权限不足消息
  - 用户未找到消息
  - 审批拒绝消息
  - 用户列表消息
  - 群消息（不含敏感信息）

---

## 快速开始

### 系统要求

- Python 3.7+
- 网络访问：钉钉开放平台、Synology NAS
- 权限：Synology 管理员账户（SSH访问权限）
- 钉钉应用：已创建钉钉应用机器人，获取 Client ID 和 Client Secret

### 安装依赖

```bash
pip install -r requirements.txt
```

或使用 Python 3：

```bash
pip3 install -r requirements.txt
```

### 配置文件

1. 复制配置示例：
```bash
cp config.ini.example config.ini
```

2. 编辑配置文件 `config.ini`：

```ini
[synology]
url = https://your-nas-ip:5001
username = admin
password = your_password
ssh_port = 22

[dingtalk]
client_id = your_client_id
client_secret = your_client_secret
# 钉钉交互卡片模板ID（在卡片平台创建后获取，可选）
card_template_id = your_card_template_id_here
# 管理员姓名（拥有创建用户权限的管理员）
admin_name = 管理员姓名
# 审批人姓名（拥有审批权限的审批人，如果不配置则默认使用管理员作为审批人）
approver_name = 审批人姓名
```

### 启动应用

```bash
python main.py
```

或

```bash
python3 main.py
```

### 验证安装

```bash
python3 -c "from src.app import DingTalkBotApp; print('✅ 安装成功')"
```

---

## 配置说明

### Synology 配置

| 配置项 | 说明 | 必填 | 默认值 |
|--------|------|------|--------|
| url | Synology NAS 地址（完整URL） | ✅ | - |
| username | 管理员用户名 | ✅ | - |
| password | 管理员密码 | ✅ | - |
| ssh_port | SSH 端口 | ❌ | 22 |

**注意**：`url` 配置项会被自动解析为 `host`，用于 SSH 连接。

### 钉钉配置

| 配置项 | 说明 | 必填 | 默认值 |
|--------|------|------|--------|
| client_id | 钉钉机器人 Client ID | ✅ | - |
| client_secret | 钉钉机器人 Client Secret | ✅ | - |
| card_template_id | 审批卡片模板 ID | ❌ | - |
| admin_name | 管理员姓名 | ✅ | "蒋成博" |
| approver_name | 审批人姓名 | ❌ | admin_name |

**说明**：
- `card_template_id`：如果配置了，系统将使用审批模式；如果未配置，将使用直接创建模式
- `admin_name`：拥有创建用户权限的管理员姓名
- `approver_name`：拥有审批权限的审批人姓名，如果不配置则默认使用管理员作为审批人

### 配置示例

```ini
[synology]
url = https://172.16.70.17:5001
username = admin
password = your_secure_password
ssh_port = 22

[dingtalk]
client_id = dingxxxxxxxxxxxxx
client_secret = your_client_secret_here
card_template_id = xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.schema
admin_name = 张三
approver_name = 李四
```

---

## 使用指南

### 通过钉钉机器人创建用户

#### 1. 找到机器人
在钉钉中搜索并添加机器人到个人聊天或群聊。

#### 2. 发送创建请求
支持以下格式：
- 直接输入姓名：`张三`
- 使用命令格式：`创建 张三`
- 在群聊中：`张三@机器人` 或 `创建 张三@机器人`

#### 3. 等待审批（如果启用了审批流程）
- 审批人会收到审批卡片
- 点击"通过"或"拒绝"
- 如果通过，系统会自动创建账户

#### 4. 接收账户信息
- 用户会通过私聊收到账户信息
- 包含：
  - 用户名（自动生成的拼音）
  - 密码（8位随机密码）
  - macOS 和 Windows 挂载脚本下载链接

### 查看用户列表

发送以下任意命令：
- `用户列表`
- `查看用户`
- `列出用户`
- `所有用户`

系统会返回 Synology 上的所有用户列表（只显示有描述的用户，过滤系统用户）。

### 权限说明

- ✅ **管理员**：可以创建用户和查看用户列表
- ✅ **审批人**：可以审批账户创建请求（仅在审批模式下）
- ❌ **其他用户**：尝试使用会收到权限不足提示
- 🔒 **账户信息**：仅通过私聊发送，不会在群聊中显示

### 安全最佳实践

1. **配置文件安全**
   - 不要将 `config.ini` 提交到版本控制
   - 使用强密码
   - 定期更换密钥

2. **权限控制**
   - 只授权必要的管理员
   - 管理员和审批人分离（推荐）
   - 定期审计创建的用户

3. **日志管理**
   - 定期检查日志文件（`logs/` 目录）
   - 监控异常行为
   - 保留日志用于审计

---

## 开发文档

### 设计原则

#### SOLID 原则

- ✅ **单一职责原则** - 每个类只负责一个功能领域
- ✅ **开闭原则** - 对扩展开放，对修改封闭
- ✅ **里氏替换原则** - 服务可以轻松替换实现
- ✅ **接口隔离原则** - 小而专注的接口
- ✅ **依赖倒置原则** - 依赖抽象而非具体实现

#### 设计模式

1. **依赖注入 (Dependency Injection)**
   - 所有服务通过构造函数注入依赖
   - 提高可测试性和可维护性

2. **服务层模式 (Service Layer)**
   - 业务逻辑封装在服务层
   - 与表示层分离

3. **策略模式 (Strategy)**
   - 用户查找使用多种策略（搜索API、部门遍历）
   - 易于扩展新策略

4. **模板方法模式 (Template Method)**
   - 消息格式化使用统一模板
   - 便于维护和扩展

### 代码结构说明

#### 消息处理流程

1. **消息接收** (`ChatbotHandler.process`)
   - 接收钉钉 Stream 消息
   - 解析消息内容

2. **权限检查** (`ChatbotHandler._check_admin_permission`)
   - 检查发送者是否为管理员
   - 支持 userid 和姓名双重验证

3. **意图解析** (`ChatbotHandler._parse_intent`)
   - 识别用户意图（创建用户/查看列表）
   - 提取参数（姓名等）

4. **用户查找** (`DingTalkUserService.find_user_by_name`)
   - 多策略查找钉钉用户
   - 返回用户 ID

5. **账户创建** (`SynologyService.create_user_from_name`)
   - 生成用户名（拼音）
   - 生成密码（随机）
   - 创建 Synology 用户
   - 加入 vlf 组

6. **消息发送** (`DingTalkMessageService.send_private_markdown`)
   - 发送账户信息给用户
   - 发送群回复（如适用）

#### 卡片审批流程

1. **卡片发送** (`DingTalkCardService.send_approval_card`)
   - 创建审批卡片
   - 发送给审批人
   - 保存卡片信息到内存

2. **卡片回调** (`CardCallbackHandler.process`)
   - 接收卡片回调事件
   - 检查审批权限
   - 解析用户操作（通过/拒绝）

3. **审批处理**
   - **通过**：创建账户 → 发送通知 → 更新卡片状态
   - **拒绝**：发送拒绝通知 → 更新卡片状态

4. **状态更新** (`DingTalkCardService.update_card_status`)
   - 更新卡片显示状态
   - 记录操作者信息

### 添加新功能

#### 添加新的消息类型

1. 在 `src/utils/message_formatter.py` 添加格式化方法：
```python
@staticmethod
def format_new_message_type(param1, param2):
    return "# 新消息类型\n\n内容..."
```

2. 在处理器中调用：
```python
message = MessageFormatter.format_new_message_type(p1, p2)
self.message_service.send_private_markdown(user_id, None, "标题", message)
```

#### 添加新的通知渠道

1. 创建新的服务类 `src/services/email_service.py`：
```python
class EmailService:
    def send_email(self, to, subject, content):
        # 实现邮件发送逻辑
        pass
```

2. 在 `src/app.py` 中初始化并注入到处理器

#### 添加新的用户查找策略

在 `src/services/dingtalk_user_service.py` 的 `find_user_by_name` 方法中添加新策略：
```python
def find_user_by_name(self, name: str) -> Optional[str]:
    # 现有策略...
    
    # 新策略
    userid = self._search_user_by_new_method(name, access_token)
    if userid:
        return userid
```

### 代码规范

- 使用 Type Hints 提供类型提示
- 所有公共方法必须有文档字符串（docstring）
- 遵循 PEP 8 代码风格
- 单个方法不超过 50 行（尽量）
- 单个类不超过 300 行（尽量）
- 使用 logging 记录日志，不使用 print

### 测试

```bash
# 运行所有测试
pytest tests/

# 运行特定测试
pytest tests/test_synology_service.py

# 查看测试覆盖率
pytest --cov=src tests/
```

---

## 部署运维

### 系统要求

- Python 3.7+
- 网络访问：钉钉开放平台、Synology NAS
- 权限：Synology 管理员账户（SSH访问权限）
- 操作系统：Linux / macOS / Windows

### 部署步骤

1. **准备环境**
```bash
git clone <repository>
cd synology
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

2. **配置应用**
```bash
cp config.ini.example config.ini
# 编辑 config.ini，填入实际配置
```

3. **启动服务**
```bash
python main.py
```

### 使用 systemd 部署（Linux）

创建服务文件 `/etc/systemd/system/synology-bot.service`：

```ini
[Unit]
Description=Synology User Management Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/synology
ExecStart=/path/to/venv/bin/python main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable synology-bot
sudo systemctl start synology-bot
sudo systemctl status synology-bot
```

### 日志管理

日志文件位置：`logs/` 目录（如果配置了文件日志）

查看日志：
```bash
tail -f logs/app.log
```

查看 systemd 日志：
```bash
journalctl -u synology-bot -f
```

### 监控

建议监控指标：
- 应用运行状态（systemd status）
- 用户创建成功率
- API 调用延迟
- 错误日志数量
- SSH 连接状态

### 故障排查

1. **应用无法启动**
   - 检查配置文件是否存在且格式正确
   - 检查 Python 版本（需要 3.7+）
   - 检查依赖是否安装完整

2. **无法连接钉钉**
   - 检查网络连接
   - 检查 Client ID 和 Client Secret 是否正确
   - 查看日志中的错误信息

3. **无法连接 Synology**
   - 检查网络连接
   - 检查 SSH 端口是否正确
   - 检查用户名和密码是否正确
   - 检查 Synology 是否允许 SSH 连接

4. **用户创建失败**
   - 检查用户是否已存在
   - 检查 Synology 管理员权限
   - 查看日志获取详细错误信息

---

## 常见问题

### Q: 如何禁用审批流程？

A: 在 `config.ini` 中删除或注释掉 `card_template_id` 配置项。系统将自动使用直接创建模式。

### Q: 如何添加多个管理员？

A: 当前版本只支持单个管理员。如需多个管理员，需要修改 `ChatbotHandler._check_admin_permission` 方法，支持多个管理员姓名或 userid。

### Q: 用户创建失败怎么办？

A: 检查以下几点：
1. Synology SSH 连接是否正常
2. 管理员账户是否有权限
3. 用户名是否已存在
4. 查看日志文件获取详细错误信息

### Q: 如何修改密码生成规则？

A: 编辑 `src/utils/password_generator.py` 中的 `generate()` 方法，修改密码长度或字符集。

### Q: 支持哪些 Synology 系统版本？

A: 已在 DSM 7.x 测试通过，理论上支持所有支持 SSH 和 `synouser` 命令的 DSM 版本。

### Q: 如何修改用户名生成规则？

A: 编辑 `src/utils/name_utils.py` 中的 `to_pinyin()` 方法，修改拼音转换逻辑。

### Q: 如何备份和恢复？

A: 备份以下内容：
- `config.ini` - 配置文件
- `logs/` - 日志文件（可选）

恢复时复制回对应位置即可。用户数据存储在 Synology NAS 上，无需备份。

### Q: 卡片审批不工作怎么办？

A: 检查以下几点：
1. 卡片模板 ID 是否正确
2. 审批人 userid 是否正确初始化（查看日志）
3. 卡片平台配置是否正确
4. 查看日志中的错误信息

---

## 附录

### 依赖包说明

| 包名 | 版本 | 用途 |
|------|------|------|
| requests | >=2.28.0 | HTTP 请求 |
| urllib3 | <2.0.0 | URL 处理 |
| paramiko | >=2.12.0 | SSH 连接 |
| dingtalk-stream | >=0.8.0 | 钉钉 Stream SDK |
| pypinyin | >=0.49.0 | 拼音转换 |
| alibabacloud-dingtalk | >=2.0.0 | 钉钉卡片 SDK |

### 术语表

- **DSM**: DiskStation Manager - Synology 操作系统
- **Stream 模式**: 钉钉长连接推送模式，实时接收消息和回调
- **卡片回调**: 用户点击卡片按钮触发的事件
- **VLF 组**: vlf 用户组，预配置了权限，新用户会自动加入此组
- **Access Token**: 钉钉 API 访问令牌，用于调用钉钉 API
- **Client ID/Secret**: 钉钉应用的凭证，用于获取 Access Token
- **Card Template ID**: 钉钉卡片模板 ID，用于创建审批卡片

### API 端点

#### 钉钉 API
- Token 获取: `https://oapi.dingtalk.com/gettoken`
- 用户搜索: `https://oapi.dingtalk.com/topapi/smartwork/hrm/employee/queryonjob`
- 用户信息: `https://oapi.dingtalk.com/topapi/v2/user/get`
- 部门列表: `https://oapi.dingtalk.com/topapi/v2/department/listsub`
- 部门用户: `https://oapi.dingtalk.com/topapi/v2/user/list`
- 私聊消息: `https://api.dingtalk.com/v1.0/robot/oToMessages/batchSend`
- 群聊消息: `https://api.dingtalk.com/v1.0/robot/groupMessages/send`
- 卡片创建: `alibabacloud-dingtalk` SDK
- 卡片更新: `alibabacloud-dingtalk` SDK

#### Synology 命令
- 创建用户: `sudo /usr/syno/sbin/synouser --add <username> <password> <fullname> 0 <email> default`
- 加入组: `sudo /usr/syno/sbin/synogroup --member <group> <username>`
- 检查用户: `id <username>`
- 用户列表: `cat /etc/passwd`

---

**文档版本**: v6.0.0   
**最后更新**: 2024年  
**作者**: jiangcb  
**许可证**: 内部使用
